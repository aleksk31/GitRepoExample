package org.example;

/**
 * Hello world!
 *
 */
public class App 
{
    // we need to initialize the system control versions.
    // it is a console version, but can do it from interface as well
    // after commit files are fixed but still local
    //
    public static void main( String[] args )
    {
        System.out.println( "Hello World!" );
    }
}
